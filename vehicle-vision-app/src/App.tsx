import React from "react";
import { CaptureImage } from "./components/capture-image/capture-image";

const App: React.FC = () => {
  return (
    <div>
      <CaptureImage />
    </div>
  );
};

export default App;
